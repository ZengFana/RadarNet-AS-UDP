From 19947b5d63718a6c3b56bfaead1ee6799fee06ca Mon Sep 17 00:00:00 2001
From: "ZYL_Mac17,9" <kurodaniji@gmail.com>
Date: Thu, 6 Aug 2026 12:49:13 +0800
Subject: [PATCH] no message

---
 .idea/vcs.xml       |  6 ++++
 radar_server_gui.py | 84 ++++++++++++++++++++++++++++++---------------
 2 files changed, 62 insertions(+), 28 deletions(-)
 create mode 100644 .idea/vcs.xml

diff --git a/.idea/vcs.xml b/.idea/vcs.xml
new file mode 100644
index 0000000..94a25f7
--- /dev/null
+++ b/.idea/vcs.xml
@@ -0,0 +1,6 @@
+<?xml version="1.0" encoding="UTF-8"?>
+<project version="4">
+  <component name="VcsDirectoryMappings">
+    <mapping directory="$PROJECT_DIR$" vcs="Git" />
+  </component>
+</project>
\ No newline at end of file
diff --git a/radar_server_gui.py b/radar_server_gui.py
index e26ee95..22d68e8 100755
--- a/radar_server_gui.py
+++ b/radar_server_gui.py
@@ -1,7 +1,7 @@
 """
 radar_server_gui.py
 ===================
-發送端控制面板：管理雷達 COM Port、提供 Browse 按鈕選擇 CFG 檔、透過 UDP Socket 傳輸 JSON 資料。
+發送端主控台：管理雷達 COM Port、選擇 CFG 設定檔，並自動紀錄連線的 Client 進行 UDP 廣播。
 """
 import sys
 import json
@@ -9,22 +9,23 @@ import socket
 import time
 from pathlib import Path
 from PySide6.QtCore import QThread, Signal, Slot
-from PySide6.QtWidgets import (QApplication, QMainWindow, QWidget, QVBoxLayout, 
-                             QHBoxLayout, QGroupBox, QFormLayout, QComboBox, 
+from PySide6.QtWidgets import (QApplication, QMainWindow, QWidget, QVBoxLayout,
+                             QHBoxLayout, QGroupBox, QFormLayout, QComboBox,
                              QSpinBox, QLineEdit, QPushButton, QPlainTextEdit, QLabel, QFileDialog)
 from serial_manager import SerialManager, SerialConfig
 from parser_as import parse_packet, AreaScannerParser
 
-class RadarServerWorker(QThread):
+class MultiClientUDPServerWorker(QThread):
     log_signal = Signal(str)
     status_signal = Signal(str)
 
-    def __init__(self, config: SerialConfig, ip="127.0.0.1", port=9999, parent=None):
+    def __init__(self, config: SerialConfig, ip="0.0.0.0", port=9999, parent=None):
         super().__init__(parent)
         self.config = config
         self.ip = ip
         self.port = port
         self._running = False
+        self.clients = {}  # 紀錄 Client 位址 -> 最後心跳時間
 
     def stop(self):
         self._running = False
@@ -34,24 +35,45 @@ class RadarServerWorker(QThread):
         parser = AreaScannerParser()
         self._running = True
 
+        # 建立 UDP Socket 並綁定 Server 監聽 Port
         udp_socket = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
-        target_addr = (self.ip, self.port)
+        udp_socket.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
+        udp_socket.bind((self.ip, self.port))
+        udp_socket.settimeout(0.01) # 設定小 Timeout 以非阻塞方式接收 Client 握手
+
+        self.log_signal.emit(f"[Server] UDP 伺服器已啟動，監聽 {self.ip}:{self.port}...")
+        self.status_signal.emit("初始化雷達...")
 
         try:
-            self.status_signal.emit("正在開啟序列埠...")
-            self.log_signal.emit(f"[Server] 開啟 CLI={self.config.cli_port}, DATA={self.config.data_port}")
             manager.open_ports()
             manager.clear_buffers()
-            
-            self.status_signal.emit("正在傳送 CFG...")
+
             self.log_signal.emit("[Server] 傳送雷達 CFG 設定檔...")
             for line in manager.send_cfg_file():
                 self.log_signal.emit(f"  {line}")
 
-            self.status_signal.emit("資料 UDP 串流中 (Streaming)")
-            self.log_signal.emit(f"[Server] 開始透過 UDP 發送至 {self.ip}:{self.port}...")
+            self.status_signal.emit("雷達傳輸中 (等待 Client 連線)")
 
             while self._running:
+                # 1. 檢查是否有 Client 發送 握手/心跳 訊號 (Ping/Connect)
+                try:
+                    data, addr = udp_socket.recvfrom(1024)
+                    msg = data.decode('utf-8').strip()
+                    if msg.startswith("CONNECT") or msg.startswith("PING"):
+                        if addr not in self.clients:
+                            self.log_signal.emit(f"[Server] 新增 Client 連線: {addr}")
+                        self.clients[addr] = time.time()  # 更新 Client 心跳時間
+                except socket.timeout:
+                    pass
+
+                # 2. 清理超過 5 秒沒有心跳的超時 Client
+                now = time.time()
+                dead_clients = [addr for addr, last_ts in self.clients.items() if now - last_ts > 5.0]
+                for addr in dead_clients:
+                    del self.clients[addr]
+                    self.log_signal.emit(f"[Server] Client 逾時中斷: {addr}")
+
+                # 3. 讀取雷達數據並發送給所有已註冊的 Client
                 raw = manager.read_data_once(max_bytes=8192)
                 if not raw:
                     self.msleep(3)
@@ -66,9 +88,17 @@ class RadarServerWorker(QThread):
                     try:
                         frame = parse_packet(packet)
                         frame_dict = frame.to_dict()
-                        
                         json_bytes = json.dumps(frame_dict).encode('utf-8')
-                        udp_socket.sendto(json_bytes, target_addr)
+
+                        # 廣播給目前線上的所有 Client
+                        for client_addr in list(self.clients.keys()):
+                            udp_socket.sendto(json_bytes, client_addr)
+
+                        if self.clients:
+                            self.status_signal.emit(f"串流中 (線上 Client: {len(self.clients)} 個)")
+                        else:
+                            self.status_signal.emit("等待 Client 連線...")
+
                     except Exception as exc:
                         self.log_signal.emit(f"[Server 解析警告]: {exc}")
 
@@ -86,17 +116,17 @@ class RadarServerWorker(QThread):
 class ServerMainWindow(QMainWindow):
     def __init__(self):
         super().__init__()
-        self.setWindowTitle("RadarNet-AS 發送端控制面板 (UDP Mode)")
-        self.resize(650, 520)
+        self.setWindowTitle("RadarNet-AS 發送端主控台 (Multi-Client UDP)")
+        self.resize(680, 540)
         self.worker = None
 
         central = QWidget(self)
         self.setCentralWidget(central)
         layout = QVBoxLayout(central)
 
-        box_serial = QGroupBox("雷達硬體與網路參數設定")
+        box_serial = QGroupBox("Server 網路與雷達硬體參數設定")
         form = QFormLayout(box_serial)
-        
+
         self.combo_cli = QComboBox()
         self.combo_data = QComboBox()
         for info in SerialManager.list_available_ports():
@@ -112,18 +142,17 @@ class ServerMainWindow(QMainWindow):
         self.spin_data_b.setRange(9600, 3000000)
         self.spin_data_b.setValue(921600)
 
+        # CFG 選擇 Browse... 按鈕
         cfg_widget = QWidget()
         cfg_layout = QHBoxLayout(cfg_widget)
         cfg_layout.setContentsMargins(0, 0, 0, 0)
-        
         self.edit_cfg = QLineEdit()
-        self.edit_cfg.setPlaceholderText("點擊 Browse... 按鈕選取 .cfg 檔案")
+        self.edit_cfg.setPlaceholderText("點擊 Browse... 選擇 .cfg 檔案")
         self.btn_browse = QPushButton("Browse...")
-        
         cfg_layout.addWidget(self.edit_cfg)
         cfg_layout.addWidget(self.btn_browse)
 
-        self.edit_ip = QLineEdit("127.0.0.1")
+        self.edit_ip = QLineEdit("0.0.0.0")  # 0.0.0.0 允許本機與局域網所有 IP 連線
         self.edit_port = QSpinBox()
         self.edit_port.setRange(1024, 65535)
         self.edit_port.setValue(9999)
@@ -133,8 +162,8 @@ class ServerMainWindow(QMainWindow):
         form.addRow("CLI 波特率 (Baud)", self.spin_cli_b)
         form.addRow("DATA 波特率 (Baud)", self.spin_data_b)
         form.addRow("CFG 設定檔路徑", cfg_widget)
-        form.addRow("目標 IP 地址", self.edit_ip)
-        form.addRow("UDP 傳輸 Port", self.edit_port)
+        form.addRow("Server 監聽 IP", self.edit_ip)
+        form.addRow("Server 監聽 Port", self.edit_port)
         layout.addWidget(box_serial)
 
         h_btn = QHBoxLayout()
@@ -153,7 +182,6 @@ class ServerMainWindow(QMainWindow):
         self.txt_log.setReadOnly(True)
         layout.addWidget(self.txt_log)
 
-        # 訊號綁定
         self.btn_browse.clicked.connect(self.browse_cfg_file)
         self.btn_test.clicked.connect(self.test_conn)
         self.btn_start.clicked.connect(self.start_server)
@@ -189,7 +217,7 @@ class ServerMainWindow(QMainWindow):
         if not cfg_path:
             self.txt_log.appendPlainText("[錯誤] 請先點擊 'Browse...' 選擇 .cfg 檔案！")
             return
-            
+
         s_config = SerialConfig(
             cli_port=self.combo_cli.currentText(),
             data_port=self.combo_data.currentText(),
@@ -197,7 +225,7 @@ class ServerMainWindow(QMainWindow):
             data_baud=self.spin_data_b.value(),
             cfg_file=cfg_path
         )
-        self.worker = RadarServerWorker(s_config, ip=self.edit_ip.text().strip(), port=self.edit_port.value())
+        self.worker = MultiClientUDPServerWorker(s_config, ip=self.edit_ip.text().strip(), port=self.edit_port.value())
         self.worker.log_signal.connect(self.txt_log.appendPlainText)
         self.worker.status_signal.connect(lambda txt: self.lbl_status.setText(f"狀態: {txt}"))
         self.worker.finished.connect(self.on_worker_finished)
@@ -222,4 +250,4 @@ if __name__ == "__main__":
     app = QApplication(sys.argv)
     win = ServerMainWindow()
     win.show()
-    sys.exit(app.exec())
\ No newline at end of file
+    sys.exit(app.exec())
-- 
2.46.0

From 067f56c22fcb9d8737e79f73b3d60371b0548252 Mon Sep 17 00:00:00 2001
From: "ZYL_Mac17,9" <kurodaniji@gmail.com>
Date: Thu, 6 Aug 2026 12:49:29 +0800
Subject: [PATCH] Revert "no message"

This reverts commit 19947b5d63718a6c3b56bfaead1ee6799fee06ca.
---
 .idea/vcs.xml       |  6 ----
 radar_server_gui.py | 84 +++++++++++++++------------------------------
 2 files changed, 28 insertions(+), 62 deletions(-)
 delete mode 100644 .idea/vcs.xml

diff --git a/.idea/vcs.xml b/.idea/vcs.xml
deleted file mode 100644
index 94a25f7..0000000
--- a/.idea/vcs.xml
+++ /dev/null
@@ -1,6 +0,0 @@
-<?xml version="1.0" encoding="UTF-8"?>
-<project version="4">
-  <component name="VcsDirectoryMappings">
-    <mapping directory="$PROJECT_DIR$" vcs="Git" />
-  </component>
-</project>
\ No newline at end of file
diff --git a/radar_server_gui.py b/radar_server_gui.py
index 22d68e8..e26ee95 100755
--- a/radar_server_gui.py
+++ b/radar_server_gui.py
@@ -1,7 +1,7 @@
 """
 radar_server_gui.py
 ===================
-發送端主控台：管理雷達 COM Port、選擇 CFG 設定檔，並自動紀錄連線的 Client 進行 UDP 廣播。
+發送端控制面板：管理雷達 COM Port、提供 Browse 按鈕選擇 CFG 檔、透過 UDP Socket 傳輸 JSON 資料。
 """
 import sys
 import json
@@ -9,23 +9,22 @@ import socket
 import time
 from pathlib import Path
 from PySide6.QtCore import QThread, Signal, Slot
-from PySide6.QtWidgets import (QApplication, QMainWindow, QWidget, QVBoxLayout,
-                             QHBoxLayout, QGroupBox, QFormLayout, QComboBox,
+from PySide6.QtWidgets import (QApplication, QMainWindow, QWidget, QVBoxLayout, 
+                             QHBoxLayout, QGroupBox, QFormLayout, QComboBox, 
                              QSpinBox, QLineEdit, QPushButton, QPlainTextEdit, QLabel, QFileDialog)
 from serial_manager import SerialManager, SerialConfig
 from parser_as import parse_packet, AreaScannerParser
 
-class MultiClientUDPServerWorker(QThread):
+class RadarServerWorker(QThread):
     log_signal = Signal(str)
     status_signal = Signal(str)
 
-    def __init__(self, config: SerialConfig, ip="0.0.0.0", port=9999, parent=None):
+    def __init__(self, config: SerialConfig, ip="127.0.0.1", port=9999, parent=None):
         super().__init__(parent)
         self.config = config
         self.ip = ip
         self.port = port
         self._running = False
-        self.clients = {}  # 紀錄 Client 位址 -> 最後心跳時間
 
     def stop(self):
         self._running = False
@@ -35,45 +34,24 @@ class MultiClientUDPServerWorker(QThread):
         parser = AreaScannerParser()
         self._running = True
 
-        # 建立 UDP Socket 並綁定 Server 監聽 Port
         udp_socket = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
-        udp_socket.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
-        udp_socket.bind((self.ip, self.port))
-        udp_socket.settimeout(0.01) # 設定小 Timeout 以非阻塞方式接收 Client 握手
-
-        self.log_signal.emit(f"[Server] UDP 伺服器已啟動，監聽 {self.ip}:{self.port}...")
-        self.status_signal.emit("初始化雷達...")
+        target_addr = (self.ip, self.port)
 
         try:
+            self.status_signal.emit("正在開啟序列埠...")
+            self.log_signal.emit(f"[Server] 開啟 CLI={self.config.cli_port}, DATA={self.config.data_port}")
             manager.open_ports()
             manager.clear_buffers()
-
+            
+            self.status_signal.emit("正在傳送 CFG...")
             self.log_signal.emit("[Server] 傳送雷達 CFG 設定檔...")
             for line in manager.send_cfg_file():
                 self.log_signal.emit(f"  {line}")
 
-            self.status_signal.emit("雷達傳輸中 (等待 Client 連線)")
+            self.status_signal.emit("資料 UDP 串流中 (Streaming)")
+            self.log_signal.emit(f"[Server] 開始透過 UDP 發送至 {self.ip}:{self.port}...")
 
             while self._running:
-                # 1. 檢查是否有 Client 發送 握手/心跳 訊號 (Ping/Connect)
-                try:
-                    data, addr = udp_socket.recvfrom(1024)
-                    msg = data.decode('utf-8').strip()
-                    if msg.startswith("CONNECT") or msg.startswith("PING"):
-                        if addr not in self.clients:
-                            self.log_signal.emit(f"[Server] 新增 Client 連線: {addr}")
-                        self.clients[addr] = time.time()  # 更新 Client 心跳時間
-                except socket.timeout:
-                    pass
-
-                # 2. 清理超過 5 秒沒有心跳的超時 Client
-                now = time.time()
-                dead_clients = [addr for addr, last_ts in self.clients.items() if now - last_ts > 5.0]
-                for addr in dead_clients:
-                    del self.clients[addr]
-                    self.log_signal.emit(f"[Server] Client 逾時中斷: {addr}")
-
-                # 3. 讀取雷達數據並發送給所有已註冊的 Client
                 raw = manager.read_data_once(max_bytes=8192)
                 if not raw:
                     self.msleep(3)
@@ -88,17 +66,9 @@ class MultiClientUDPServerWorker(QThread):
                     try:
                         frame = parse_packet(packet)
                         frame_dict = frame.to_dict()
+                        
                         json_bytes = json.dumps(frame_dict).encode('utf-8')
-
-                        # 廣播給目前線上的所有 Client
-                        for client_addr in list(self.clients.keys()):
-                            udp_socket.sendto(json_bytes, client_addr)
-
-                        if self.clients:
-                            self.status_signal.emit(f"串流中 (線上 Client: {len(self.clients)} 個)")
-                        else:
-                            self.status_signal.emit("等待 Client 連線...")
-
+                        udp_socket.sendto(json_bytes, target_addr)
                     except Exception as exc:
                         self.log_signal.emit(f"[Server 解析警告]: {exc}")
 
@@ -116,17 +86,17 @@ class MultiClientUDPServerWorker(QThread):
 class ServerMainWindow(QMainWindow):
     def __init__(self):
         super().__init__()
-        self.setWindowTitle("RadarNet-AS 發送端主控台 (Multi-Client UDP)")
-        self.resize(680, 540)
+        self.setWindowTitle("RadarNet-AS 發送端控制面板 (UDP Mode)")
+        self.resize(650, 520)
         self.worker = None
 
         central = QWidget(self)
         self.setCentralWidget(central)
         layout = QVBoxLayout(central)
 
-        box_serial = QGroupBox("Server 網路與雷達硬體參數設定")
+        box_serial = QGroupBox("雷達硬體與網路參數設定")
         form = QFormLayout(box_serial)
-
+        
         self.combo_cli = QComboBox()
         self.combo_data = QComboBox()
         for info in SerialManager.list_available_ports():
@@ -142,17 +112,18 @@ class ServerMainWindow(QMainWindow):
         self.spin_data_b.setRange(9600, 3000000)
         self.spin_data_b.setValue(921600)
 
-        # CFG 選擇 Browse... 按鈕
         cfg_widget = QWidget()
         cfg_layout = QHBoxLayout(cfg_widget)
         cfg_layout.setContentsMargins(0, 0, 0, 0)
+        
         self.edit_cfg = QLineEdit()
-        self.edit_cfg.setPlaceholderText("點擊 Browse... 選擇 .cfg 檔案")
+        self.edit_cfg.setPlaceholderText("點擊 Browse... 按鈕選取 .cfg 檔案")
         self.btn_browse = QPushButton("Browse...")
+        
         cfg_layout.addWidget(self.edit_cfg)
         cfg_layout.addWidget(self.btn_browse)
 
-        self.edit_ip = QLineEdit("0.0.0.0")  # 0.0.0.0 允許本機與局域網所有 IP 連線
+        self.edit_ip = QLineEdit("127.0.0.1")
         self.edit_port = QSpinBox()
         self.edit_port.setRange(1024, 65535)
         self.edit_port.setValue(9999)
@@ -162,8 +133,8 @@ class ServerMainWindow(QMainWindow):
         form.addRow("CLI 波特率 (Baud)", self.spin_cli_b)
         form.addRow("DATA 波特率 (Baud)", self.spin_data_b)
         form.addRow("CFG 設定檔路徑", cfg_widget)
-        form.addRow("Server 監聽 IP", self.edit_ip)
-        form.addRow("Server 監聽 Port", self.edit_port)
+        form.addRow("目標 IP 地址", self.edit_ip)
+        form.addRow("UDP 傳輸 Port", self.edit_port)
         layout.addWidget(box_serial)
 
         h_btn = QHBoxLayout()
@@ -182,6 +153,7 @@ class ServerMainWindow(QMainWindow):
         self.txt_log.setReadOnly(True)
         layout.addWidget(self.txt_log)
 
+        # 訊號綁定
         self.btn_browse.clicked.connect(self.browse_cfg_file)
         self.btn_test.clicked.connect(self.test_conn)
         self.btn_start.clicked.connect(self.start_server)
@@ -217,7 +189,7 @@ class ServerMainWindow(QMainWindow):
         if not cfg_path:
             self.txt_log.appendPlainText("[錯誤] 請先點擊 'Browse...' 選擇 .cfg 檔案！")
             return
-
+            
         s_config = SerialConfig(
             cli_port=self.combo_cli.currentText(),
             data_port=self.combo_data.currentText(),
@@ -225,7 +197,7 @@ class ServerMainWindow(QMainWindow):
             data_baud=self.spin_data_b.value(),
             cfg_file=cfg_path
         )
-        self.worker = MultiClientUDPServerWorker(s_config, ip=self.edit_ip.text().strip(), port=self.edit_port.value())
+        self.worker = RadarServerWorker(s_config, ip=self.edit_ip.text().strip(), port=self.edit_port.value())
         self.worker.log_signal.connect(self.txt_log.appendPlainText)
         self.worker.status_signal.connect(lambda txt: self.lbl_status.setText(f"狀態: {txt}"))
         self.worker.finished.connect(self.on_worker_finished)
@@ -250,4 +222,4 @@ if __name__ == "__main__":
     app = QApplication(sys.argv)
     win = ServerMainWindow()
     win.show()
-    sys.exit(app.exec())
+    sys.exit(app.exec())
\ No newline at end of file
-- 
2.46.0

